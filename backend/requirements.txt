fastapi==0.109.0
uvicorn==0.27.0
motor==3.3.2
pymongo==4.6.1
pydantic==2.5.3
python-dotenv==1.0.0
python-multipart==0.0.6

# Test dependencies.
# httpx < 0.28 is required: starlette 0.35's TestClient passes `app=` to
# httpx.Client, an argument httpx 0.28 removed (TypeError at client setup).
httpx==0.26.0
pytest==7.4.4
