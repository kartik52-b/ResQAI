<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GPS location access -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Background location (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Phone calls -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Internet (backend) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Wake lock for background processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Microphone for voice emergency detection -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Vibration for alerts -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:usesCleartextTraffic="true"
        android:label="ResQ AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Emergency popup: full-screen activity shown over lock screen -->
        <activity
            android:name=".EmergencyPopupActivity"
            android:exported="false"
            android:theme="@style/EmergencyPopupTheme"
            android:launchMode="singleInstance"
            android:taskAffinity="com.example.resq_ai.emergency"
            android:excludeFromRecents="true"
            android:screenOrientation="portrait"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <!-- Background monitoring foreground service -->
        <service
            android:name=".ResQMonitoringService"
            android:exported="false"
            android:foregroundServiceType="location|microphone" />

        <!-- Receiver for emergency popup button actions -->
        <receiver
            android:name=".EmergencyResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.resq_ai.EMERGENCY_OK" />
                <action android:name="com.example.resq_ai.EMERGENCY_HELP" />
                <action android:name="com.example.resq_ai.EMERGENCY_TIMEOUT" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="package" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>
